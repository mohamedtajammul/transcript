# Redis (to listen for jobs)
REDIS_HOST=redis
REDIS_PORT=6379

# MinIO (to fetch uploaded audio files)
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=uploads

# OpenAI API key (for Whisper transcription)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
# MongoDB (to update transcript in DB)
MONGO_URI=mongodb://mongo:27017/omind_ai

# Service-specific
PORT=5001